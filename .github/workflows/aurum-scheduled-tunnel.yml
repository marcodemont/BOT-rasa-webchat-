name: AURUM Scheduled Tunnel

on:
  workflow_dispatch:
  schedule:
    # Schweiz (CEST, UTC+2): 06:30, 09:00, 11:45, 14:30, 18:00
    - cron: '30 4 * * *'
    - cron: '0 7 * * *'
    - cron: '45 9 * * *'
    - cron: '30 12 * * *'
    - cron: '0 16 * * *'

concurrency:
  group: aurum-scheduled-tunnel
  cancel-in-progress: true

jobs:
  slot_0630:
    if: github.event_name == 'workflow_dispatch' || github.event.schedule == '30 4 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 70
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Start AURUM + Tunnel
        shell: bash
        env:
          AURUM_TUNNEL_ID: ${{ secrets.AURUM_TUNNEL_ID }}
          AURUM_TUNNEL_CREDENTIALS_JSON: ${{ secrets.AURUM_TUNNEL_CREDENTIALS_JSON }}
          AURUM_TUNNEL_HOSTNAME: aurum.me.marcodemont.ch
        run: |
          set -euo pipefail
          npm ci

          mkdir -p "$HOME/.cloudflared"
          printf '%s' "$AURUM_TUNNEL_CREDENTIALS_JSON" > "$HOME/.cloudflared/${AURUM_TUNNEL_ID}.json"
          cat > "$HOME/.cloudflared/aurum.yml" <<EOF
          tunnel: ${AURUM_TUNNEL_ID}
          credentials-file: ${HOME}/.cloudflared/${AURUM_TUNNEL_ID}.json
          ingress:
            - hostname: ${AURUM_TUNNEL_HOSTNAME}
              service: http://localhost:8006
            - service: http_status:404
          EOF

          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
            -o "$HOME/cloudflared"
          chmod +x "$HOME/cloudflared"

          npm run dev -- --host 0.0.0.0 --port 8006 --strictPort > aurum-dev.log 2>&1 &
          DEV_PID=$!
          "$HOME/cloudflared" tunnel --config "$HOME/.cloudflared/aurum.yml" run > cloudflared.log 2>&1 &
          TUNNEL_PID=$!

          trap 'kill $DEV_PID $TUNNEL_PID 2>/dev/null || true' EXIT

          while true; do
            sleep 30
            kill -0 "$DEV_PID"
            kill -0 "$TUNNEL_PID"
          done

  slot_0900:
    if: github.event.schedule == '0 7 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Start AURUM + Tunnel
        shell: bash
        env:
          AURUM_TUNNEL_ID: ${{ secrets.AURUM_TUNNEL_ID }}
          AURUM_TUNNEL_CREDENTIALS_JSON: ${{ secrets.AURUM_TUNNEL_CREDENTIALS_JSON }}
          AURUM_TUNNEL_HOSTNAME: aurum.me.marcodemont.ch
        run: |
          set -euo pipefail
          npm ci

          mkdir -p "$HOME/.cloudflared"
          printf '%s' "$AURUM_TUNNEL_CREDENTIALS_JSON" > "$HOME/.cloudflared/${AURUM_TUNNEL_ID}.json"
          cat > "$HOME/.cloudflared/aurum.yml" <<EOF
          tunnel: ${AURUM_TUNNEL_ID}
          credentials-file: ${HOME}/.cloudflared/${AURUM_TUNNEL_ID}.json
          ingress:
            - hostname: ${AURUM_TUNNEL_HOSTNAME}
              service: http://localhost:8006
            - service: http_status:404
          EOF

          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
            -o "$HOME/cloudflared"
          chmod +x "$HOME/cloudflared"

          npm run dev -- --host 0.0.0.0 --port 8006 --strictPort > aurum-dev.log 2>&1 &
          DEV_PID=$!
          "$HOME/cloudflared" tunnel --config "$HOME/.cloudflared/aurum.yml" run > cloudflared.log 2>&1 &
          TUNNEL_PID=$!

          trap 'kill $DEV_PID $TUNNEL_PID 2>/dev/null || true' EXIT

          while true; do
            sleep 30
            kill -0 "$DEV_PID"
            kill -0 "$TUNNEL_PID"
          done

  slot_1145:
    if: github.event.schedule == '45 9 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 105
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Start AURUM + Tunnel
        shell: bash
        env:
          AURUM_TUNNEL_ID: ${{ secrets.AURUM_TUNNEL_ID }}
          AURUM_TUNNEL_CREDENTIALS_JSON: ${{ secrets.AURUM_TUNNEL_CREDENTIALS_JSON }}
          AURUM_TUNNEL_HOSTNAME: aurum.me.marcodemont.ch
        run: |
          set -euo pipefail
          npm ci

          mkdir -p "$HOME/.cloudflared"
          printf '%s' "$AURUM_TUNNEL_CREDENTIALS_JSON" > "$HOME/.cloudflared/${AURUM_TUNNEL_ID}.json"
          cat > "$HOME/.cloudflared/aurum.yml" <<EOF
          tunnel: ${AURUM_TUNNEL_ID}
          credentials-file: ${HOME}/.cloudflared/${AURUM_TUNNEL_ID}.json
          ingress:
            - hostname: ${AURUM_TUNNEL_HOSTNAME}
              service: http://localhost:8006
            - service: http_status:404
          EOF

          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
            -o "$HOME/cloudflared"
          chmod +x "$HOME/cloudflared"

          npm run dev -- --host 0.0.0.0 --port 8006 --strictPort > aurum-dev.log 2>&1 &
          DEV_PID=$!
          "$HOME/cloudflared" tunnel --config "$HOME/.cloudflared/aurum.yml" run > cloudflared.log 2>&1 &
          TUNNEL_PID=$!

          trap 'kill $DEV_PID $TUNNEL_PID 2>/dev/null || true' EXIT

          while true; do
            sleep 30
            kill -0 "$DEV_PID"
            kill -0 "$TUNNEL_PID"
          done

  slot_1430:
    if: github.event.schedule == '30 12 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 70
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Start AURUM + Tunnel
        shell: bash
        env:
          AURUM_TUNNEL_ID: ${{ secrets.AURUM_TUNNEL_ID }}
          AURUM_TUNNEL_CREDENTIALS_JSON: ${{ secrets.AURUM_TUNNEL_CREDENTIALS_JSON }}
          AURUM_TUNNEL_HOSTNAME: aurum.me.marcodemont.ch
        run: |
          set -euo pipefail
          npm ci

          mkdir -p "$HOME/.cloudflared"
          printf '%s' "$AURUM_TUNNEL_CREDENTIALS_JSON" > "$HOME/.cloudflared/${AURUM_TUNNEL_ID}.json"
          cat > "$HOME/.cloudflared/aurum.yml" <<EOF
          tunnel: ${AURUM_TUNNEL_ID}
          credentials-file: ${HOME}/.cloudflared/${AURUM_TUNNEL_ID}.json
          ingress:
            - hostname: ${AURUM_TUNNEL_HOSTNAME}
              service: http://localhost:8006
            - service: http_status:404
          EOF

          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
            -o "$HOME/cloudflared"
          chmod +x "$HOME/cloudflared"

          npm run dev -- --host 0.0.0.0 --port 8006 --strictPort > aurum-dev.log 2>&1 &
          DEV_PID=$!
          "$HOME/cloudflared" tunnel --config "$HOME/.cloudflared/aurum.yml" run > cloudflared.log 2>&1 &
          TUNNEL_PID=$!

          trap 'kill $DEV_PID $TUNNEL_PID 2>/dev/null || true' EXIT

          while true; do
            sleep 30
            kill -0 "$DEV_PID"
            kill -0 "$TUNNEL_PID"
          done

  slot_1800:
    if: github.event.schedule == '0 16 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Start AURUM + Tunnel
        shell: bash
        env:
          AURUM_TUNNEL_ID: ${{ secrets.AURUM_TUNNEL_ID }}
          AURUM_TUNNEL_CREDENTIALS_JSON: ${{ secrets.AURUM_TUNNEL_CREDENTIALS_JSON }}
          AURUM_TUNNEL_HOSTNAME: aurum.me.marcodemont.ch
        run: |
          set -euo pipefail
          npm ci

          mkdir -p "$HOME/.cloudflared"
          printf '%s' "$AURUM_TUNNEL_CREDENTIALS_JSON" > "$HOME/.cloudflared/${AURUM_TUNNEL_ID}.json"
          cat > "$HOME/.cloudflared/aurum.yml" <<EOF
          tunnel: ${AURUM_TUNNEL_ID}
          credentials-file: ${HOME}/.cloudflared/${AURUM_TUNNEL_ID}.json
          ingress:
            - hostname: ${AURUM_TUNNEL_HOSTNAME}
              service: http://localhost:8006
            - service: http_status:404
          EOF

          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
            -o "$HOME/cloudflared"
          chmod +x "$HOME/cloudflared"

          npm run dev -- --host 0.0.0.0 --port 8006 --strictPort > aurum-dev.log 2>&1 &
          DEV_PID=$!
          "$HOME/cloudflared" tunnel --config "$HOME/.cloudflared/aurum.yml" run > cloudflared.log 2>&1 &
          TUNNEL_PID=$!

          trap 'kill $DEV_PID $TUNNEL_PID 2>/dev/null || true' EXIT

          while true; do
            sleep 30
            kill -0 "$DEV_PID"
            kill -0 "$TUNNEL_PID"
          done
